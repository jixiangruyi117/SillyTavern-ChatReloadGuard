# 开发记录

## 2026-08-12

- 现象：移动端点击聊天输入框或收起软键盘时出现短暂停顿，收起后原键盘区域可能短暂为黑色。
- 根因证据：SillyTavern 1.18.0 的 `public/scripts/browser-fixes.js` 会在每次移动端 `window.resize` 时将 `document.documentElement.style.position` 设为 `fixed` 并在下一帧清除；软键盘视口变化会触发该路径，同时移动端 `body` 固定、主要区域使用 `100dvh` 与模糊合成层。
- 初版反证：仅撤销根节点临时固定定位后，用户真机仍卡顿；因此该写入不是主因，相关实现已完整撤回，不保留叠加补丁。
- 新的根因证据：用户截图显示软键盘收起后输入栏仍停在键盘打开时的 Y 坐标，`#sheld` 在该处结束并露出整块 `body` 黑底；这直接指向 `100dvh` 在该浏览器没有恢复应用壳体高度。
- 方案：不尝试移除酒馆匿名监听器，也不阻止全局 `resize`。扩展在用户手动开启后将 `visualViewport.height` 以像素值同步到 `body`、`#bg1`、`#sheld`，并在关闭开关时恢复原有行内样式；其他监听器继续运行。
- 时序保护：输入框失焦后立即、120ms、360ms 各读取一次真实可视高度，防止部分 Android 浏览器在键盘动画末尾才提交新高度。
- 验证：Node 回归覆盖键盘打开、收起、关闭开关恢复原行为和原生 resize 监听器仍运行。真机仍需在可丢弃聊天上覆盖 Android/iOS 的打开、收起、横竖屏和候选栏输入。

## 2026-08-09

- 根因：SillyTavern 1.18.0 的正则预设切换会调用聊天重载；重载先清空内存聊天，再读取服务器。读取失败后会按新聊天流程保存开场白，从而覆盖原聊天文件。
- 方案：不修改酒馆核心，只替换已导出的 `reloadChatMutex.callback`，并在单次重载期间保护当前聊天的读取与保存。
- 兼容：仅允许已审计的 1.18.0 且函数结构吻合时启用；其他版本不接管并提示。
- 验证：Node 故障注入测试、语法检查、本机酒馆 8011 端口静态资源加载检查，以及 Edge 真实页面激活检查。
- 实机结果：设置页显示“保护中”，识别版本 `1.18.0 (51ad27f)`，激活钩子完成且 mutex 已接管。
- 验证修正：酒馆上下文不直接暴露 `clearChat/printMessages`，因此改为显式注入酒馆原生导出，并用单消息异常重载测试覆盖备用恢复分支。
- 本次复核：补充端到端故障注入，完整模拟 `clearChat` 后读取 503、空聊天初始化和开场白保存，确认保护器返回原快照且保存请求为 0；另验证不同聊天的插件保存请求不被拦截。
- 文档发布：README 只依据现有源码、测试和上述实机结果扩写，明确支持范围、已知边界和二次开发约束。
- 授权与反馈：按维护者决定加入未经改写的 `PolyForm Noncommercial License 1.0.0` 正式文本，并使用 SPDX 标识写入 `package.json`；README 首部说明由 GPT 辅助整理，内容问题统一通过仓库 GitHub Issues 反馈。
