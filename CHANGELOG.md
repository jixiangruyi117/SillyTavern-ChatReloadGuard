# 更新记录

## 未发布 - 2026-08-12

- 版本提升至 `0.3.2`：只将输入框、输入控件和可编辑元素的焦点变化识别为键盘周期，设置页按钮不再污染诊断报告。
- 版本提升至 `0.3.1`：诊断仍可在内存保留 120 条，但“复制精简诊断”只导出最近一次键盘过程的最多 18 个关键事件，并合并重复布局信息，避免报告过长无法发送。
- 版本提升至 `0.3.0`，新增默认关闭的“修复移动端软键盘高度残留”。
- 修复不依赖键盘收起后可能仍未更新的视口值，而是在输入聚焦前保存稳定高度，并在失焦时立即恢复聊天壳体。
- 新增默认关闭的只读键盘诊断：内存中最多保留 120 条尺寸、坐标、焦点、修复动作和长任务事件，可复制或清空，不读取聊天和输入内容。
- 增加“视口数值仍旧时失焦恢复稳定高度”和诊断隐私边界回归。

- 增加完整故障链回归，锁定“读取失败后误建开场白并保存”的原始问题。
- 增加跨聊天请求兼容回归，确认保护期内其他插件操作不同聊天时继续透传。
- 扩写 README，补充根因、保护范围、状态说明、恢复建议、验证方法及二次开发安全边界。
- 采用 `PolyForm-Noncommercial-1.0.0` 软件许可，允许非商业使用、修改与分发，并在 README 首部增加 GPT 辅助整理及 GitHub Issues 反馈说明。
- 运行时保护逻辑不变；现有 0.1.0 已覆盖该根因。

## 0.1.0 - 2026-08-09

- 首次提供 SillyTavern 1.18.0 聊天重载保护。
- 在切换正则或预设触发重载时保留内存快照。
- 对读取失败、畸形响应、异常缩短和危险保存执行保护。
- 版本或内部实现不匹配时停止接管并持续提示用户。
