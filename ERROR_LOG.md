# 问题记录

## 2026-08-12：失焦后的长任务仍混入已完成的键盘周期

- 证据：`0.3.4` 报告已出现 `keyboard-focusout`，但约 9 秒后点击复制按钮产生的连续 51–125ms 长任务仍被写入同一周期。
- 根因：周期依赖失焦后 700ms 的异步计时器才关闭；在该设备上该关闭没有可靠阻断后续 `PerformanceObserver` 回调。
- 修复：`0.3.5` 在失焦当刻将活动周期置空，立即拒绝后续 resize/长任务；已排定的失焦后 100/300/600ms 采样通过独立的 settling 状态完成。
- 回归要求：失焦之后、600ms 采样完成之前和之后新增的长任务都不得进入报告；仍必须保留三个已排定的收起后采样点。

## 2026-08-12：诊断与修复器在未发生视口变化时仍制造重复布局工作

- 证据：真实键盘周期中全部高度保持 765px，但保护器仍多次写入同一个 `765px`；诊断对每个 `window/visualViewport resize` 都读取四个元素的矩形和计算样式，并额外安排三次延时完整读取。
- 风险：相同样式写入可触发无意义的样式失效；高频同步布局读取可能放大长聊天的主线程阻塞，影响对原始卡顿的判断。
- 修复：`0.3.4` 只有高度值变化时才写样式；完整布局采样仅保留键盘焦点锚点和固定延时点，resize 改为 80ms 合并的轻量事件，不读取矩形/计算样式。
- 回归要求：相同高度的焦点更新不得再写 CSS 变量；同一时间窗内 `window` 与 `visualViewport` resize 只记录一条轻量合并事件。

## 2026-08-12：精简报告跨越了键盘周期，混入复制操作

- 现象：`0.3.2` 虽以真实输入框的 `keyboard-focusin` 开始，但从该事件一直截取到复制时刻；报告混入复制按钮触发的长任务和无关 resize，且可能在压缩时丢掉即时 `keyboard-focusout`。
- 根因：诊断记录没有周期归属和关闭时点，导出仅按最近一次 `keyboard-focusin` 切片。
- 修复：`0.3.3` 为每次输入框聚焦生成独立周期，只记录到失焦后的 600ms 采样完成；导出只选择最新周期，并优先保留即时聚焦/失焦锚点。
- 回归要求：周期结束后新增的长任务不得进入报告；报告必须保留同周期的输入框聚焦和失焦事件。

## 2026-08-12：诊断把设置页按钮焦点误判为软键盘周期

- 现象：报告中的 `focusin` 焦点是“复制精简诊断”按钮，所有视口高度相同，不能反映输入框开合；因此报告中的长任务不能归因给软键盘。
- 根因：扩展在 `document` 上监听了所有 `focusin/focusout`，没有检查事件目标是否可编辑。
- 修复：`0.3.2` 仅对文本类 `input`、`textarea` 和 `contenteditable` 目标记录 `keyboard-focusin/keyboard-focusout`；按钮、复选框、选择框与普通页面焦点不再触发延时采样。
- 回归要求：按钮焦点不新增键盘事件；输入框焦点必须成为精简报告的首个事件。

## 2026-08-12：只读诊断报告过长，无法通过聊天发送

- 现象：内存上限 120 条事件被完整、缩进导出，每条又重复携带四组计算样式，导致用户无法发送报告。
- 根因：混淆了“本机环形记录容量”和“对外诊断载荷”；复制操作没有提取最近一次复现过程，也没有压缩重复字段。
- 修复：`0.3.1` 继续在内存保留 120 条，只复制最近一次 `focusin` 起的最多 18 个关键事件；完整计算样式只保留末态一份，事件字段使用带说明的紧凑结构。
- 回归要求：构造 120 条记录后，导出事件不得超过 18 条，报告必须小于 6000 字符，并继续验证不含聊天或输入内容。

## 2026-08-12：两次移动端软键盘缓解均未通过真机验证

- 现象：Android 设备打开输入框时卡顿；收起软键盘后输入栏可能停在键盘打开时的位置，底部露出大块黑色区域。
- 已否定方案一：仅撤销 SillyTavern 移动端 `resize` 回调写入的根节点 `position: fixed`。用户开启后症状不变。
- 已否定方案二：依据截图将 `visualViewport.height` 写入 `body`、`#bg1`、`#sheld`，并在失焦后延迟重试。用户真机仍确认症状不变。
- 已执行：两个无效提交均已 revert，聊天重载保护原功能保留；不再保留开关或视口覆盖逻辑。
- 后续取证要求：在同一设备上记录 `window.innerHeight`、`visualViewport.height`、`#sheld`/`#chat`/`#form_sheld` 的 `getBoundingClientRect()`、每次 resize 的时间线和 Performance 长任务，分别覆盖键盘打开、收起、黑块出现与恢复。未取得这些运行时证据前不得新增第三个修复补丁。
- 后续执行：用户明确要求先按源码缺口修复并同时增加只读诊断。`0.3.0` 因而只采用前两次未覆盖的“键盘前稳定高度”策略，并把上述运行时字段内置为可复制报告；在真机报告返回前不得声明问题已经修好。

## 2026-08-09：聊天重载失败后覆盖历史记录

- 现象：切换正则或含正则预设后，聊天偶发只剩开场白。
- 触发链：正则预设应用 → `reloadCurrentChat()` → `clearChat({ clearData: true })` → 聊天读取失败 → 新聊天初始化 → 保存开场白。
- 风险点：清空发生在读取成功之前；失败分支仍允许整份聊天写回。
- 回归要求：读取失败不得减少当前聊天，重载期间不得把多条消息保存成更短聊天，未知酒馆版本不得自动接管。

