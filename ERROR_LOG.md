# 问题记录

## 2026-08-12：移动端软键盘切换触发短暂卡顿或黑色空白

- 现象：点击聊天输入框弹出软键盘、或取消输入收起软键盘时页面卡顿；后者可能更久，原键盘区域短暂显示黑色空白。
- 已确认源码链：移动端 `window.resize` 会触发 `fixFunkyPositioning()`，暂时固定根节点；同一过渡还会重算固定 `body`、`100dvh` 高度和带模糊的聊天区域。
- 处理边界：静态源码证据不能替代具体设备复现。扩展仅提供默认关闭的可逆缓解，不删除酒馆监听器、不停止事件传播；真机验证失败时必须关闭开关、撤回该缓解并重新采集性能与布局证据。
- 回归要求：验证软键盘打开、收起、横竖屏、候选词替换、关闭开关和其他扩展 resize 监听器；不得用全局 `stopImmediatePropagation()` 作为修复。

## 2026-08-09：聊天重载失败后覆盖历史记录

- 现象：切换正则或含正则预设后，聊天偶发只剩开场白。
- 触发链：正则预设应用 → `reloadCurrentChat()` → `clearChat({ clearData: true })` → 聊天读取失败 → 新聊天初始化 → 保存开场白。
- 风险点：清空发生在读取成功之前；失败分支仍允许整份聊天写回。
- 回归要求：读取失败不得减少当前聊天，重载期间不得把多条消息保存成更短聊天，未知酒馆版本不得自动接管。
