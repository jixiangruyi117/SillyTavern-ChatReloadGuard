{
  "name": "sillytavern-chat-reload-guard",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check index.js && node --check modules/ChatReloadGuardCore.js && node --test tests/*.test.mjs"
  }
}
